# Copy to .env and fill in. .env is gitignored — never commit real keys.

# --- Hosted free tier -------------------------------------------------------
# Free key, no credit card: https://openrouter.ai  → Keys
OPENROUTER_API_KEY=sk-or-...

# A free model that supports tool calling. Check the current free list at
# https://openrouter.ai/models?q=free — the :free suffix is required.
MODEL=nvidia/nemotron-ultra-253b-v1:free

# --- Local models (optional, fully offline) ---------------------------------
# Install https://ollama.com then: ollama pull llama3.2
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2

# --- Optional, only for stretch goals ---------------------------------------
# PUSHOVER_TOKEN=
# PUSHOVER_USER=
