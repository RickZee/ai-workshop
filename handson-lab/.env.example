# Copy to .env and fill in. .env is gitignored — never commit real keys.

# --- Hosted free tier -------------------------------------------------------
# Free key, no credit card: https://openrouter.ai  → Keys
OPENROUTER_API_KEY=sk-or-...
# Optional: point at any OpenAI-compatible endpoint instead (proxy, gateway, other provider)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# A free model that supports tool calling — Labs 3, 5 and 6 need tool support.
# The free list ROTATES: ids are retired without notice. Catalog listing "tools"
# is not enough (some ids 403 except inside a coding agent). Lab 0 lists what is
# free+tool-capable today, then fires a real tool call — trust that second cell.
#
# Live-checked 2026-09-03 against a real OpenRouter key (well-formed tool calls):
#   inclusionai/ling-3.0-flash-fin:free
#   nvidia/nemotron-3.5-lightning:free
# Avoid: thinkingmachines/inkling-small:free (403 on chat completions)
MODEL=inclusionai/ling-3.0-flash-fin:free

# --- Local models (optional, fully offline) ---------------------------------
# Install https://ollama.com then: ollama pull llama3.2
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2

# --- Optional, only for stretch goals ---------------------------------------
# PUSHOVER_TOKEN=
# PUSHOVER_USER=
